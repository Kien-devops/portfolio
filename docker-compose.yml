services:
  portfolio:
    build:
      context: .
    image: kien-portfolio:latest
    container_name: kien-portfolio
    restart: unless-stopped
    ports:
      - "3001:8080"
    env_file:
      - .env.local
