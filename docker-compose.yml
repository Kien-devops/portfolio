services:
  portfolio:
    build:
      context: .
    image: kien-portfolio:latest
    container_name: kien-portfolio
    restart: unless-stopped
    network_mode: host
    environment:
      - PORT=3001
    env_file:
      - .env.local
